import { Component } from '@angular/core';

@Component({
    selector: 'app-header', // Use a unique selector for your component
    standalone: true, // This is a standalone component
    templateUrl: './header.component.html', // Assuming you have an HTML template for this component
    styleUrls: ['./header.component.css'] // Assuming you have styles specific to this component
})
export class HeaderComponent {}
